
public class ThirdFuncCalc {
    public double calculate(ThirdFuncParam param) {
        return (3 * (Math.log(Math.abs(param.a/param.b)) + Math.sqrt(Math.cos(param.c) + Math.pow(Math.E, param.d))) );
    }
}
